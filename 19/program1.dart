//annonymous function //Lambda Function

//lambda fun returns the object of annonymous class and we call that object


var add = (int a, int b){      //add = object and all body after equals to is a class
	return a+b;
};

void main() {
	print(add(10,20)); // object call for annonymous function
}